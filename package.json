{
  "name": "github-action-issue-to-jira",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "start": "node ./index.js",
    "test": "jest",
    "build": "npx @vercel/ncc build && npx convert-action"
  },
  "dependencies": {
    "@actions/core": "1.10.0",
    "actions-toolkit": "6.0.1",
    "jira-client": "8.2.2"
  },
  "devDependencies": {
    "jest": "^24.5.0",
    "nock": "^12.0.0"
  }
}
